[package]
name = "task-store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
task-core = { path = "../task-core" }

[lib]
path = "src/lib.rs"
