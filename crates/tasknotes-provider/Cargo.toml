[package]
name = "tasknotes-provider"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
task-core = { path = "../task-core" }
task-store = { path = "../task-store" }
task-sync = { path = "../task-sync" }

[lib]
path = "src/lib.rs"
